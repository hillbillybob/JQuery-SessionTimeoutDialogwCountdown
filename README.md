# JQuery-SessionTimeoutDialogwCountdown
This is a JQuery dialog box with countdown functionality to handle session timeouts, warning the user with a countdown, and committing the timeout upon completion.
